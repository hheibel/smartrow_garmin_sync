name: Build and Deploy to Cloud Run Job

# Trigger the workflow on pushes to the main branch or manually via the GitHub UI
on:
  push:
    branches:
      - main
  workflow_dispatch:

# Define environment variables (Update these with your specific GCP details)
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true    # Forces GitHub Actions to run under Node 24
  PROJECT_ID: garmin-syncher-491619           # e.g., web-scraper-bot-4192a
  REGION: europe-west3                        # e.g., us-central1
  GAR_REPOSITORY: garmin-syncher-repository   # Name of your Artifact Registry repository
  JOB_NAME: garmin-syncher-job                # Name for your Cloud Run Job
  # Workload Identity Federation details
  WIF_PROVIDER: projects/28251301164/locations/global/workloadIdentityPools/github-actions-pool/providers/github-provider
  SERVICE_ACCOUNT: github-deployer@garmin-syncher-491619.iam.gserviceaccount.com

jobs:
  deploy:
    # Use Ubuntu runner
    runs-on: ubuntu-latest

    # These permissions are required for Workload Identity Federation to work
    permissions:
      contents: 'read'
      id-token: 'write'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Securely authenticate to Google Cloud without a JSON key
      - name: Authenticate to Google Cloud
        id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ env.WIF_PROVIDER }}
          service_account: ${{ env.SERVICE_ACCOUNT }}

      # Configure Docker to use GCP Artifact Registry
      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker Authentication
        run: |-
          gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev --quiet

      # Build the Docker image and tag it with the GitHub commit SHA
      - name: Build Docker Image
        run: |-
          docker build -t ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/${{ env.JOB_NAME }}:${{ github.sha }} .

      # Push the image to Google Artifact Registry
      - name: Push Docker Image
        run: |-
          docker push ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/${{ env.JOB_NAME }}:${{ github.sha }}

      # Deploy / Update the Cloud Run Job
      - name: Deploy Cloud Run Job
        run: |-
          gcloud run jobs deploy ${{ env.JOB_NAME }} \
            --image ${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.GAR_REPOSITORY }}/${{ env.JOB_NAME }}:${{ github.sha }} \
            --region ${{ env.REGION }} \
            --set-env-vars GCP_PROJECT_ID=${{ env.PROJECT_ID }} \
            --max-retries 1 \
            --task-timeout 10m